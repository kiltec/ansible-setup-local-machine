# Setup a local Ubuntu system with Ansible

## Usage ##
 - Install Ansible
 - ```ansible-galaxy install -r requirements.yml```
 - ```sudo ansible-playbook main.yml```
 
## Todos ##
 - [ ] Get rid of literal home directory
 - [ ] Write better README
 - [ ] check if it's good to run the playbook with `sudo` 
